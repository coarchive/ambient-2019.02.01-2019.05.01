# ambient/linux/common/bin

Programs stored within [ambient/linux/common](..) are hardlinked into this directory.
[ambient/linux/spawn/paths.sh](../../spawn/paths.sh) adds this directory to the `$PATH`
